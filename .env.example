# Plex and NFS CSI Driver Configuration
# Copy this file to your project root as .env and fill in your actual values

# Synology NAS Configuration
SYNOLOGY_NAS_IP=the-nas-ip
SYNOLOGY_NAS_SHARE=/volume2/plex

# Plex Configuration
PLEX_CLAIM_TOKEN=the-claim-token
PLEX_LOADBALANCER_IP=192.168.68.221
PLEX_SERVER_NAME=plex-k3s

# Storage Configuration
PLEX_MEDIA_STORAGE_SIZE=1Ti
PLEX_CONFIG_STORAGE_SIZE=20Gi

# Usage Instructions:
# 1. Copy this file to your project root as .env
# 2. Update the values with your actual configuration
# 3. Use these environment variables in your Kubernetes manifests
# 4. Never commit the actual .env file to version control
